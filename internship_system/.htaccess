Options -Indexes
RewriteEngine On

# Cho phép truy cập trực tiếp file/thư mục thực sự tồn tại (uploads, css, js, ...)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tất cả request còn lại → front controller
RewriteRule ^ index.php [L]
